import { IFormGraph } from '@formily/core/esm/types';
import { Form } from '@formily/core/esm/models/Form';
export declare class Graph {
    form: Form;
    constructor(form: Form);
    getGraph: () => IFormGraph;
    setGraph: (graph: IFormGraph) => void;
}
