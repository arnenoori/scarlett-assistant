import { useRef } from 'react';

var useImmutable = function useImmutable(val) {
  var r = useRef(val);
  r.current = val; // useEffect(() => {
  //   r.current = val;
  // });

  return r;
};

export default useImmutable;